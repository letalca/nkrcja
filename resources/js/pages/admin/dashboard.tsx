import { Main } from '@/components/main';
import Layout from '@/layouts/layout';

export default function Dashboard() {
    return (
        <Layout>
            <Main fixed>
                <p>Dashboard Page</p>
            </Main>
        </Layout>
    );
}
